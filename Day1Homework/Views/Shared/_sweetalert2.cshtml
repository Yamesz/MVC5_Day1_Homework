@model AlertViewModel
@{ 
    if(Model == null || string.IsNullOrWhiteSpace(Model.Title))
    {
        return;
    }
}
<script>
    swal(
      '@Model.Title',
      '@Model.Msg',
      '@Model.Status'
    );
</script>
